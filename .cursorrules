# Cursor AI 代码生成规则 - lyrixi-mobile

## 🚫 强制规则

### 1. 必须使用项目现有组件
**绝对禁止创建新的组件！** 必须从以下列表中选择并使用现有组件：

#### 布局组件
- `App` - 应用容器
- `Page` - 页面容器
- `Card` - 卡片容器
- `Flex` - 弹性布局（支持 gap、align、wrap 等）
- `Row` - 行布局
- `Modal` - 模态框（必须先加载）
- `ActionSheet` - 操作面板
- `Float` - 浮动元素
- `SafeArea` - 安全区域

#### 导航组件
- `NavBar` - 导航栏
- `TabBar` - 标签栏
- `IndexBar` - 索引栏
- `Steps` - 步骤条
- `FooterBar` - 底部栏
- `ToolBar` - 工具栏（筛选、搜索、操作栏等）

#### 表单组件
- `Form` - 表单容器
- `Input` - 输入框（包含 Text, Textarea, Number, Password, Search, Tel, Url, AutoSize, Range, Rate, OTP, Select 等子组件）
- `Button` - 按钮
- `Checkbox` - 复选框
- `Switch` - 开关
- `Picker` - 选择器
- `Select` - 选择框
- `Cascader` - 级联选择器
- `DatePicker` - 日期选择器（包含 RangeCombo, RangeMain, WeekCombo 等）
- `Selector` - 选择器
- `Transfer` - 穿梭框
- `Signature` - 签名
- `Attach` - 附件选择/展示
- `AttachUploader` - 附件上传
- `Media` - 媒体选择/展示
- `MediaUploader` - 媒体上传
- `Location` - 位置选择
- `Keyboard` - 键盘（数字键盘等）

#### 展示组件
- `Text` - 文本（含 Ellipsis 等）
- `Icon` - 图标
- `Badge` - 徽章
- `Divider` - 分割线
- `Result` - 结果页
- `Skeleton` - 骨架屏
- `Loading` - 加载中
- `Progress` - 进度条
- `Toast` - 轻提示
- `Message` - 消息提示
- `Tooltip` - 工具提示
- `NoticeBar` - 通知栏
- `Accordion` - 手风琴
- `List` - 列表
- `ListAsync` - 异步/虚拟列表
- `ListPagination` - 分页列表
- `Stamp` - 印章
- `Amount` - 金额展示
- `Combo` - 组合组件（多控件组合）
- `QRCode` - 二维码
- `IFrame` - 内嵌框架
- `VideoPlayer` - 视频播放器

#### 数据展示
- `Calendar` - 日历
- `Map` - 地图
- `Chat` - 聊天（对话列表等）

#### 反馈组件
- `Modal` - 对话框
- `ActionSheet` - 操作面板
- `Toast` - 轻提示
- `Message` - 消息提示
- `Loading` - 加载中

### 2. 必须使用项目现有工具
**绝对禁止创建新的工具函数！** 必须从以下列表中选择并使用现有工具：

#### 工具类
- `ArrayUtil` - 数组工具（deepTree, flattenTree, searchDeepTree 等）
- `DateUtil` - 日期工具（format, add, compare, diff 等）
- `DOMUtil` - DOM 工具（classNames, preventDefault, getEventPosition, variables）
- `MathUtil` - 数学工具（variableSize 等）
- `ObjectUtil` - 对象工具（isEmpty, randomUUID 等）
- `AssetUtil` - 资源工具（loadImage, loadJs, getFileExtension 等）
- `EventUtil` - 事件工具
- `GeoUtil` - 地理工具
- `HistoryUtil` - 历史工具
- `LocaleUtil` - 国际化工具
- `Logger` - 日志工具
- `Storage` - 存储工具（localStorage, sessionStorage, useCacheState）
- `Request` - 请求工具（get, post, formatResponse, formatError）
- `Clipboard` - 剪贴板工具
- `Device` - 设备工具（getDevice, getOS, getPlatform 等）
- `Debugger` - 调试工具
- `FullScreen` - 全屏工具
- `Bridge` - 桥接工具

### 3. 导入规范

**重要：必须使用按需导入，避免全量打包！**

#### 组件导入
```javascript
// ✅ 正确：按需导入组件（避免全量打包）
import Button from 'lyrixi-mobile/components/Button'
import Card from 'lyrixi-mobile/components/Card'
import Input from 'lyrixi-mobile/components/Input'
import DatePicker from 'lyrixi-mobile/components/DatePicker'
import Modal from 'lyrixi-mobile/components/Modal'
import Page from 'lyrixi-mobile/components/Page'
import Flex from 'lyrixi-mobile/components/Flex'
import Form from 'lyrixi-mobile/components/Form'

// ✅ 正确：使用子组件
<DatePicker.RangeCombo />
<Input.Text />
<Flex.Compact />

// ❌ 错误：不要全量导入（会导致包体积过大）
// import { Button, Card, Input } from 'lyrixi-mobile'

// ❌ 错误：不要创建新组件
// const MyButton = () => <button>...</button>
```

#### 工具导入
```javascript
// ✅ 正确：按需导入工具（避免全量打包）
import DOMUtil from 'lyrixi-mobile/utils/DOMUtil'
import DateUtil from 'lyrixi-mobile/utils/DateUtil'
import MathUtil from 'lyrixi-mobile/utils/MathUtil'
import ArrayUtil from 'lyrixi-mobile/utils/ArrayUtil'
import Storage from 'lyrixi-mobile/utils/Storage'
import LocaleUtil from 'lyrixi-mobile/utils/LocaleUtil'

// ✅ 正确：使用工具方法
DOMUtil.classNames('class1', 'class2')
DateUtil.format(new Date(), 'date')
MathUtil.variableSize('m', 'space')

// ❌ 错误：不要全量导入（会导致包体积过大）
// import { DOMUtil, DateUtil, MathUtil } from 'lyrixi-mobile'

// ❌ 错误：不要创建新工具函数
// const formatDate = (date) => {...}
// const classNames = (...args) => {...}
```

#### 导入路径规范

**组件路径格式：**
- `lyrixi-mobile/components/Button`
- `lyrixi-mobile/components/Card`
- `lyrixi-mobile/components/Input`
- `lyrixi-mobile/components/DatePicker`
- `lyrixi-mobile/components/Modal`
- 等等...

**工具路径格式：**
- `lyrixi-mobile/utils/DOMUtil`
- `lyrixi-mobile/utils/DateUtil`
- `lyrixi-mobile/utils/MathUtil`
- `lyrixi-mobile/utils/ArrayUtil`
- `lyrixi-mobile/utils/Storage`
- 等等...

### 4. 代码生成要求

1. **优先使用现有组件**：如果功能可以通过现有组件实现，必须使用现有组件
2. **组合使用组件**：通过组合现有组件实现复杂功能，而不是创建新组件
3. **使用工具函数**：所有数据处理、格式化、计算等操作必须使用现有工具
4. **遵循项目规范**：
   - 使用 `DOMUtil.classNames` 处理类名
   - 使用 `DateUtil` 处理日期
   - 使用 `MathUtil.variableSize` 处理尺寸变量
   - 使用 `LocaleUtil.locale` 处理国际化

### 5. 禁止事项

❌ **绝对禁止**：
- 创建新的组件文件
- 创建新的工具函数文件
- 使用第三方 UI 库（如 antd, material-ui 等）
- 直接操作 DOM（使用 DOMUtil）
- 手动实现日期格式化（使用 DateUtil）
- 手动实现类名合并（使用 DOMUtil.classNames）

### 6. 特殊情况

如果确实需要新功能：
1. 首先检查是否可以通过组合现有组件实现
2. 检查是否有类似的组件可以扩展
3. 如果必须创建新组件，请明确说明原因，并遵循项目代码规范

---

## 📝 使用示例

### 示例 1：创建表单页面
```javascript
// ✅ 正确：按需导入
import Page from 'lyrixi-mobile/components/Page'
import Card from 'lyrixi-mobile/components/Card'
import Form from 'lyrixi-mobile/components/Form'
import Input from 'lyrixi-mobile/components/Input'
import Button from 'lyrixi-mobile/components/Button'
import DatePicker from 'lyrixi-mobile/components/DatePicker'

export default () => {
  return (
    <Page>
      <Page.Main>
        <Card>
          <Card.Header>表单标题</Card.Header>
          <Card.Main>
            <Form>
              <Form.Item label="姓名">
                <Input.Text />
              </Form.Item>
              <Form.Item label="日期">
                <DatePicker.Combo />
              </Form.Item>
              <Button>提交</Button>
            </Form>
          </Card.Main>
        </Card>
      </Page.Main>
    </Page>
  )
}
```

### 示例 2：使用工具函数
```javascript
// ✅ 正确：按需导入
import DOMUtil from 'lyrixi-mobile/utils/DOMUtil'
import DateUtil from 'lyrixi-mobile/utils/DateUtil'
import MathUtil from 'lyrixi-mobile/utils/MathUtil'

// ✅ 正确：使用工具方法
const className = DOMUtil.classNames('base-class', { 'active': isActive })
const formattedDate = DateUtil.format(new Date(), 'date')
const size = MathUtil.variableSize('m', 'space')

// ❌ 错误：不要全量导入
// import { DOMUtil, DateUtil, MathUtil } from 'lyrixi-mobile'

// ❌ 错误：不要手动实现
// const className = ['base-class', isActive && 'active'].filter(Boolean).join(' ')
// const formattedDate = new Date().toLocaleDateString()
```

---

## 📚 组件使用指南

### 重要提示
**在生成代码前，如果不确定组件的使用方法，请先查看以下文件：**
1. 组件的文档：`src/components/[ComponentName]/index.zh-CN.md`
2. 组件的示例：`src/components/[ComponentName]/demos/*.jsx`
3. 组件的实际代码：`src/components/[ComponentName]/[ComponentName].jsx`

### 常用组件 API 速查

#### Page 页面容器
```javascript
import Page from 'lyrixi-mobile/components/Page'

<Page>
  <Page.Header>标题</Page.Header>
  <Page.Main>内容</Page.Main>
  <Page.Footer>底部</Page.Footer>
</Page>
```

#### Card 卡片
```javascript
import Card from 'lyrixi-mobile/components/Card'

<Card>
  <Card.Header>标题</Card.Header>
  <Card.Main>内容</Card.Main>
</Card>
```

#### Button 按钮
```javascript
import Button from 'lyrixi-mobile/components/Button'

<Button 
  color="primary"           // 'default' | 'primary' | 'link' | 'warning' | 'danger' | 'success'
  backgroundColor="white"   // 'default' | 'white' | 'transparent' | 'primary' | ...
  size="m"                  // 'xxs' | 'xs' | 's' | 'm' | 'l' | 'xl'
  radius="m"                // 'xxs' | 'xs' | 's' | 'm' | 'l' | 'xl'
  disabled={false}
  onClick={() => {}}
>
  按钮文字
</Button>
```

#### Input 输入框
```javascript
import Input from 'lyrixi-mobile/components/Input'

// 文本输入
<Input.Text 
  placeholder="请输入"
  value={value}
  onChange={(val) => setValue(val)}
  allowClear
  maxLength={100}
/>

// 数字输入
<Input.Number 
  value={value}
  onChange={setValue}
  min={0}
  max={100}
/>

// 密码输入
<Input.Password 
  value={value}
  onChange={setValue}
/>

// 搜索框
<Input.Search 
  placeholder="搜索"
  onSearch={(value) => {}}
/>
```

#### Form 表单
```javascript
import Form from 'lyrixi-mobile/components/Form'
import Input from 'lyrixi-mobile/components/Input'
import Button from 'lyrixi-mobile/components/Button'

<Form>
  <Form.Item name="username" label="用户名">
    <Input.Text />
  </Form.Item>
  <Form.Item name="password" label="密码">
    <Input.Password />
  </Form.Item>
  <Button>提交</Button>
</Form>
```

#### DatePicker 日期选择器
```javascript
import DatePicker from 'lyrixi-mobile/components/DatePicker'
import DateUtil from 'lyrixi-mobile/utils/DateUtil'

// 基础用法
<DatePicker.Combo 
  type="date"              // 'year' | 'quarter' | 'month' | 'date' | 'time' | 'datetime'
  value={value}
  onChange={setValue}
  allowClear
/>

// 日期范围
<DatePicker.RangeCombo 
  value={[startDate, endDate]}
  onChange={setValue}
/>

// 快捷选择
<DatePicker.RangeMain 
  rangesVisible={true}
  rangeId={rangeId}
  ranges={{
    '今日': [new Date(), new Date()],
    '近7日': [DateUtil.add(new Date(), -6), new Date()],
  }}
  value={value}
  onChange={(newValue, { rangeId }) => {}}
/>
```

#### Select 选择框
```javascript
import Select from 'lyrixi-mobile/components/Select'

<Select.Combo 
  list={[
    { id: '1', name: '选项1' },
    { id: '2', name: '选项2' },
  ]}
  value={value}
  onChange={setValue}
  multiple={false}
  allowClear
/>
```

#### Modal 模态框
```javascript
import Modal from 'lyrixi-mobile/components/Modal'

<Modal 
  open={open}
  onClose={() => setOpen(false)}
  maskClosable={true}
  animation="zoom"          // 'none' | 'slideLeft' | 'slideRight' | 'slideUp' | 'slideDown' | 'zoom' | 'fade'
>
  内容
</Modal>
```

#### Toast 轻提示
```javascript
import Toast from 'lyrixi-mobile/components/Toast'

// 显示
Toast.show('提示信息', { duration: 2000 })

// 隐藏
Toast.hide()
```

#### Loading 加载中
```javascript
import Loading from 'lyrixi-mobile/components/Loading'

// 显示
Loading.show('加载中...')

// 隐藏
Loading.hide()
```

#### Flex 弹性布局
```javascript
import Flex from 'lyrixi-mobile/components/Flex'
import Button from 'lyrixi-mobile/components/Button'

<Flex 
  direction="horizontal"    // 'horizontal' | 'vertical'
  gap="m"                  // 'xxxs' | 'xxs' | 'xs' | 's' | 'm' | 'l' | 'xl' | 'xxl' | number
  align="start"            // 'start' | 'end' | 'center' | 'between' | 'around' | 'evenly'
  wrap={false}             // true | false | 'scroll'
>
  <Button>按钮1</Button>
  <Button>按钮2</Button>
</Flex>

// 紧凑布局
<Flex.Compact gap="s">
  <Button>按钮1</Button>
  <Button>按钮2</Button>
</Flex.Compact>
```

#### List 列表
```javascript
import List from 'lyrixi-mobile/components/List'

<List 
  list={[
    { id: '1', name: '项目1' },
    { id: '2', name: '项目2' },
  ]}
  value={selectedIds}
  onChange={setSelectedIds}
  multiple={false}
  checkable={true}
/>
```

### 工具函数使用指南

#### DOMUtil
```javascript
import DOMUtil from 'lyrixi-mobile/utils/DOMUtil'

// 类名合并
DOMUtil.classNames('class1', 'class2', { 'active': isActive })

// 阻止默认事件
DOMUtil.preventDefault(event)

// 获取事件位置
DOMUtil.getEventPosition(event)

// 样式变量
DOMUtil.variables.colors  // ['transparent', 'white', 'default', 'primary', ...]
DOMUtil.variables.sizes    // ['xxxs', 'xxs', 'xs', 's', 'm', 'l', 'xl', 'xxl', 'xxxl']
```

#### DateUtil
```javascript
import DateUtil from 'lyrixi-mobile/utils/DateUtil'

// 格式化日期
DateUtil.format(new Date(), 'date')        // 'YYYY-MM-DD'
DateUtil.format(new Date(), 'datetime')    // 'YYYY-MM-DD HH:mm:ss'

// 日期计算
DateUtil.add(new Date(), 7, 'day')        // 加7天
DateUtil.add(new Date(), -1, 'month')      // 减1个月

// 日期比较
DateUtil.compare(new Date(), new Date(), 'date')

// 获取开始/结束
DateUtil.startOrEnd(new Date(), 'month', 'start')
DateUtil.startOrEnd(new Date(), 'week', 'end')
```

#### MathUtil
```javascript
import MathUtil from 'lyrixi-mobile/utils/MathUtil'

// 变量尺寸转换
MathUtil.variableSize('m', 'space')       // 将 'm' 转换为对应的像素值
MathUtil.variableSize(16, 'space')        // 数字直接返回
```

#### ArrayUtil
```javascript
import ArrayUtil from 'lyrixi-mobile/utils/ArrayUtil'

// 树形结构操作
ArrayUtil.deepTree(flatArray, { idKey: 'id', parentIdKey: 'parentId' })
ArrayUtil.flattenTree(treeArray)
ArrayUtil.searchDeepTree(treeArray, (item) => item.id === targetId)
```

#### Storage
```javascript
import Storage from 'lyrixi-mobile/utils/Storage'

// 设置
Storage.setItem('key', 'value')

// 获取
Storage.getItem('key')

// 删除
Storage.removeItem('key')

// 清空
Storage.clear()
```

### 查看实际示例

如果对组件的使用有疑问，请查看以下示例文件（路径以项目为准）：

- Button: `src/components/Button/demos/Button.jsx`
- Card: `src/components/Card/demos/Card.jsx`
- Input: `src/components/Input/demos/InputText.jsx`
- Form: `src/components/Form/demos/Layout.jsx`
- DatePicker: `src/components/DatePicker/demos/DatePickerCombo.jsx`
- Select: `src/components/Select/demos/SelectCombo.jsx`
- Modal: `src/components/Modal/demos/Modal.jsx` 或 `Modal/demos/Modal/index.jsx`
- Flex: `src/components/Flex/demos/flex.jsx`
- List: `src/components/List/demos/List.jsx`

### 避免幻觉的规则

1. **不确定时查看代码**：如果对组件的 API 不确定，先查看组件的实际代码或 demo
2. **使用真实示例**：参考项目中的实际 demo 文件，不要自己编造 API
3. **遵循项目规范**：所有代码必须符合项目的代码风格和规范
4. **不要假设**：不要假设组件有某些不存在的 props 或方法
5. **查看文档**：优先查看 `index.zh-CN.md` 文档文件了解组件的完整 API

---

**记住：优先使用现有组件和工具，不确定时查看实际代码和示例，不要重复造轮子！**
