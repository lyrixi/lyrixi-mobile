import type { DatePickerComboProps, DatePickerComboRef } from './DatePicker.Combo.types'

/** 周选择 Combo，与单日期 Combo 相同 */
export interface DatePickerWeekComboProps extends DatePickerComboProps {}

export interface DatePickerWeekComboRef extends DatePickerComboRef {}
