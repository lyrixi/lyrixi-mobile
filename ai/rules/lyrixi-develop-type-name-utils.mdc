---
description: 类型定义工具方法入参出参命名规范
globs:
  - '**/*.{js,jsx,ts,tsx}'
alwaysApply: true
---

# 类型定义工具方法入参出参命名规范

## 工具方法入参

若入参已有类型, 则跳过此节, 无需执行此节的内容, 例如: `QueryParamsState`等已定义的类型.

若传入组装的数据类型, 入参类型统一使用 `Options` 结尾, 示例(`${FunctionName}Options`):

```ts
type OpenWindowOptions = {
  url: string
}
```
