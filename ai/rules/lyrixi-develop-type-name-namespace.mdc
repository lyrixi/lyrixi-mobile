---
description: 类型定义组件集合命名规范
globs:
  - '**/*.{js,jsx,ts,tsx}'
alwaysApply: true
---

# 类型定义组件集合命名规范

组件聚合类型统一使用 `Components` 结尾（禁止使用 Namespace 作为后缀）, 示例(`${ComponentName}Components`):

```ts
type ListPaginationComponents = {
  Combo: React.FC
  Modal: React.FC
  Main: React.FC
}
```
