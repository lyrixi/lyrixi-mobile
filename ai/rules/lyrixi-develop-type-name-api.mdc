---
description: 类型定义API接口相关命名规范
globs:
  - '**/*.{js,jsx,ts,tsx}'
alwaysApply: true
---

# 类型定义 API 接口相关命名规范

- 出参: 接口返回类型统一以 `Response` 结尾, 示例(`${FunctionName}Response`):

```ts
type QueryDataResponse = {
  code: '1' | '0'
  message: string
  data: T | undefined
}
```

- 入参: 调用接口时将本地数据转成服务器数据, 以`ServerParams`结尾, 示例(`${FunctionName}ServerParams`):
