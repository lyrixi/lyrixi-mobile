# FullScreen Example

以下示例位于本目录 `demos/`（由 `src/utils/FullScreen/demos` 同步，运行 `npm run build:ai-docs` 更新）。

业务代码引入：`import { FullScreen } from 'lyrixi-mobile'`

## demos/FullScreen.tsx

```tsx
import { Page } from 'lyrixi-mobile'

export default function FullScreenDemo() {
  return (
    <Page>
      <Page.Main>定位</Page.Main>
    </Page>
  )
}
```
