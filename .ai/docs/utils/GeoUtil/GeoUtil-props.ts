/**
 * GeoUtil API（AI 文档，生成代码时以此为准）
 */

export namespace GeoUtil {
  /** 获取边线, [[[lng, lat], [lng, lat]], [[lng, lat], [lng, lat]]] */

  export function for(...args: unknown[]): unknown
  /** 检查参数是否有效 */

  export function if(...args: unknown[]): unknown
  /** 判断是否在范围内 */

  export function return(...args: unknown[]): unknown
}
