# Theme Example

以下示例位于本目录 `demos/`（由 `src/utils/Theme/demos` 同步，运行 `npm run build:ai-docs` 更新）。

业务代码引入：`import { Theme } from 'lyrixi-mobile'`

_暂无 demo 文件，请查阅 `-rules.md`、`-props.ts` 与 `src/utils/Theme/`。_