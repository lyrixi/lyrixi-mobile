# ReactDOMClientCompat Example

业务代码引入：`import { ReactDOMClientCompat } from 'lyrixi-mobile'`

## Demo 索引

该工具主要用于内部兼容场景，无独立 demo。

## 查阅顺序

1. `ReactDOMClientCompat-props.ts` — API
2. `ReactDOMClientCompat-rules.md` — 何时使用