# EventUtil Example

以下示例位于本目录 `demos/`（由 `src/utils/EventUtil/demos` 同步，运行 `npm run build:ai-docs` 更新）。

业务代码引入：`import { EventUtil } from 'lyrixi-mobile'`

## demos/EventUtil.tsx

```tsx
import { Page } from 'lyrixi-mobile'

export default function EventUtilDemo() {
  return (
    <Page>
      <Page.Main>定位</Page.Main>
    </Page>
  )
}
```
