# Edit Virtual Rules

> 模板 ID：`edit-virtual` · 源码：`demos/Virtual/`

## 何时使用

- 字段很多的编辑页，Form 开启 **virtual** 虚拟滚动

## 说明

- 复用 Cache 的 `api/` 与 Footer
- `Form virtual` + 可选 `scrollerElement`

## create-page 替换点

同 Edit Common，`form.virtual` 固定为 `true`。
