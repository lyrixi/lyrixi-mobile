# Select Example

示例源码在 `demos/`（由 `src/components/Select/demos` 同步）。需要具体写法时 **Read 下表对应 demo 文件**，不要依赖本文件中的旧代码块。

业务代码引入：`import { Select } from 'lyrixi-mobile'`

## Demo 索引

| Demo | 说明 |
|------|------|
| [demos/SelectCombo.tsx](./demos/SelectCombo.tsx) | Select.Combo 子组件示例 |

## 查阅顺序

1. `Select-props.ts` — API
2. `Select-rules.md` — 何时使用、子组件
3. 上表 `demos/` — 需要片段时再读
