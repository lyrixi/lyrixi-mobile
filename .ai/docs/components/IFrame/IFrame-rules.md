# IFrame Rules

> 源文档：`src/components/IFrame/index.zh-CN.md`。更新后请执行 `npm run build:ai-docs`。

## 必须使用
- 从 `lyrixi-mobile` 引入 `IFrame`，**禁止**自造同类 UI。
- 子组件写法：`IFrame.Sub`（与库导出一致）。

## 何时使用
- 需要嵌入其他页面时
- 需要显示外部内容时
- 需要隔离内容时

## 查阅顺序
- Props：`IFrame-props.json`
- 示例：`IFrame-example.md`