# Keyboard Rules

> 源文档：`src/components/Keyboard/index.zh-CN.md`。更新后请执行 `npm run build:ai-docs`。

## 必须使用
- 从 `lyrixi-mobile` 引入 `Keyboard`，**禁止**自造同类 UI。
- 子组件写法：`Keyboard.Sub`（与库导出一致）。

## 何时使用
- 见 `src/components/Keyboard/index.zh-CN.md` 中「何时使用」。

## 子组件
- `Keyboard.Number`

## Demo 索引（本目录 `demos/`）
- `demos/KeyboardNumber.tsx`

## 查阅顺序
- Props：`Keyboard-props.ts`
- 示例：`Keyboard-example.md`