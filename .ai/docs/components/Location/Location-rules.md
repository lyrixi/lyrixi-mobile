# Location Rules

> 源文档：`src/components/Location/index.zh-CN.md`。更新后请执行 `npm run build:ai-docs`。

## 必须使用
- 从 `lyrixi-mobile` 引入 `Location`，**禁止**自造同类 UI。
- 子组件写法：`Location.Sub`（与库导出一致）。

## 何时使用
- 见 `src/components/Location/index.zh-CN.md` 中「何时使用」。

## 子组件
- `Location.Combo`
- `Location.Modal`
- `Location.Main`

## Demo 索引（本目录 `demos/`）
- `demos/LocationCombo.tsx`
- `demos/LocationMain.tsx`

## 查阅顺序
- Props：`Location-props.json`
- 示例：`Location-example.md`