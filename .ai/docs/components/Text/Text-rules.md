# Text Rules

> 源文档：`src/components/Text/index.zh-CN.md`。更新后请执行 `npm run build:ai-docs`。

## 必须使用
- 从 `lyrixi-mobile` 引入 `Text`，**禁止**自造同类 UI。
- 子组件写法：`Text.Sub`（与库导出一致）。

## 何时使用
- 需要显示文本时

## Demo 索引（本目录 `demos/`）
- `demos/Text.tsx`

## 查阅顺序
- Props：`Text-props.ts`
- 示例：`Text-example.md`