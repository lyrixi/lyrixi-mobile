# Amount Rules

> 源文档：`src/components/Amount/index.tsx`。更新后请执行 `npm run build:ai-docs`。

## 必须使用
- 从 `lyrixi-mobile` 引入 `Amount`，**禁止**自造同类 UI。

## 何时使用
- 需要格式化显示金额时
- 需要带货币符号显示金额时
- 需要千分位分隔显示金额时

## Demo 索引（本目录 `demos/`）
- `demos/Amount.tsx`

## 查阅顺序
- Props：`Amount-props.ts`
- 示例：`Amount-example.md`