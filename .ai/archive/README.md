# archive

已结束、仅作查阅的材料。

| 子目录 | 来源 | 写入方式 |
|--------|------|----------|
| `decisions/` | `decisions/` 活跃决策 | `/save-decisions` |
| `memory/` | `memory/MEMORY.md` 中过期的 `##` 小节 | `/save-memory` 归档流程 |

文件命名：`decisions/` 下为 `<标题>.md`（由 `/save-decisions` 根据内容总结生成）。
