# decisions

进行中、待用户拍板的材料：

- **AI 计划** — 方案对比、实施步骤草案
- **问题列表** — 待确认项、选项与推荐

默认编辑 `current.md`；也可按主题新增 `*.md`（勿放入 `archive/`）。

归档：运行 `/save-decisions`，将当前内容写入 `../archive/decisions/<标题>.md` 并清空活跃文件。
